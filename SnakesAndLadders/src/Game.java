public class Game {
    // Might be useful for restructuring
    //Game game = new Game(12,)
    boolean playing = true;

    public boolean notover() {
        return playing;
    }







    //while (playing){
    //  String playing_player = player_names[0];
    //Player Players.dequeue();
    // playing = false;

    //Game.notover()
    //Game game = new Game(board_size,)
    //game.firstSquare().isOccupied();
}
